sudo su
apt update
apt upgrade

apt install net-tools

apt install curl


sudo apt update
sudo apt install openssh-server

sudo systemctl enable ssh

sudo systemctl status ssh

sudo ufw allow 22


sudo ufw enable
sudo ufw status


sudo nano /etc/ssh/sshd_config

make password auth yes


sudo systemctl restart ssh
